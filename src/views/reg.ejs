<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Registro - Cha Cha</title>
    <%- include ("partials/head.ejs") %>
  </head>

  <body>
    <form action="/register" method="POST">
      <div>
        <h2>Registrate</h2>
      </div>

      <% if (message.length > 0) { %>
      <div class="alert">
        <%= message %>
      </div>
      <% } %>

      <div>
        <label for="name">Nombre</label>
        <input
          required
          type="text"
          name="name"
          placeholder="Escribe tu nombre"
          id="name"
          maxlength="18"
        />
      </div>
      <div>
        <label for="name">Correo</label>
        <input
          required
          type="email"
          name="email"
          placeholder="Escribe tu email"
          id="correo"
        />
      </div>
      <div>
        <label for="password">Contraseña</label>
        <input
          required
          name="psw"
          type="password"
          placeholder="Escribe tu contraseña"
          id="password"
          maxlength="12"
        />
        <div>
          <p class="forg">
            Al hacer click en registrar aceptas los
            <a href="#">Terminos y condiciones</a>
          </p>
          <p class="cuenta">
            Ya tienes cuenta? <a href="register.html">Ingresa</a>
          </p>
        </div>
      </div>
      <button type="submit" class="aceptar">Registrame</button>
    </form>
  </body>
</html>
